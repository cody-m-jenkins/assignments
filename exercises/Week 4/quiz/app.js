const matrix = [
    [1,2,3],
    [5,6,7],
    [[9,8,4]]
]
function retFunc() {
    return(matrix[1][2])
}
let i

function printFunc() {
    for (i=0; i <=matrix.length; i++)
    return matrix[i[i]]

}
console.log(printFunc(matrix[i]))

// function Person (name, isAlive, hairColor) {
//     this.name = name;
//     this.isAlive = isAlive;
//     this.hairColor = hairColor;
//     this.speak = function() {console.log(`${this.name}`)}
// }

// const cody = new Person("Cody Jenkins", true, "blond")

// this.speak = function() {console.log(`${this.name}`)}

// console.log(cody)